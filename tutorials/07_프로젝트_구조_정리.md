# 07. 프로젝트 구조 정리

## 목차
1. [전체 프로젝트 구조](#전체-프로젝트-구조)
2. [계층별 역할 정리](#계층별-역할-정리)
3. [주요 패턴과 원칙](#주요-패턴과-원칙)
4. [어노테이션 정리](#어노테이션-정리)
5. [학습 가이드](#학습-가이드)

---

## 전체 프로젝트 구조

```
fantasy-builder/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── dev/nihilncunia/fantasy_builder/
│   │   │       ├── FantasyBuilderApplication.java  # 메인 애플리케이션
│   │   │       ├── config/                         # 설정 클래스
│   │   │       │   └── SecurityConfig.java         # Spring Security 설정
│   │   │       └── domain/                         # 비즈니스 로직
│   │   │           ├── common/                    # 공통 기능
│   │   │           │   ├── entity/                 # 공통 엔티티
│   │   │           │   │   ├── CommonEntity.java
│   │   │           │   │   └── CommonVO.java
│   │   │           │   ├── exception/              # 예외 처리
│   │   │           │   │   ├── BusinessException.java
│   │   │           │   │   └── GlobalExceptionHandler.java
│   │   │           │   ├── response/               # 응답 구조
│   │   │           │   │   ├── ApiResponse.java
│   │   │           │   │   └── ResponseCode.java
│   │   │           │   ├── type/                  # 타입 정의
│   │   │           │   │   ├── ListType.java
│   │   │           │   │   └── UserRoleCode.java
│   │   │           │   └── util/                  # 유틸리티
│   │   │           │       ├── CookieHelper.java
│   │   │           │       ├── EmailHelper.java
│   │   │           │       ├── PageUtils.java
│   │   │           │       ├── PasswordHelper.java
│   │   │           │       └── PasswordPolicyHelper.java
│   │   │           ├── user/                      # 사용자 관리
│   │   │           │   ├── UserEntity.java
│   │   │           │   ├── UserVO.java
│   │   │           │   ├── UserRepository.java
│   │   │           │   ├── UserService.java
│   │   │           │   └── UserController.java
│   │   │           ├── auth/                      # 인증
│   │   │           │   ├── AuthService.java
│   │   │           │   ├── JwtHelper.java
│   │   │           │   └── JwtAuthenticationFilter.java
│   │   │           ├── trait/                     # 전역 특성
│   │   │           │   ├── TraitEntity.java
│   │   │           │   ├── TraitVO.java
│   │   │           │   ├── TraitRepository.java
│   │   │           │   ├── TraitService.java
│   │   │           │   └── TraitController.java
│   │   │           └── ability/                    # 전역 어빌리티
│   │   │               ├── AbilityEntity.java
│   │   │               ├── AbilityVO.java
│   │   │               ├── AbilityRepository.java
│   │   │               ├── AbilityService.java
│   │   │               └── AbilityController.java
│   │   └── resources/
│   │       └── application.yml                     # 설정 파일
│   └── test/                                        # 테스트 코드
└── tutorials/                                       # 튜토리얼 문서
```

---

## 계층별 역할 정리

### 1. Controller 계층 (API 계층)

**위치**: `domain/*/Controller.java`

**역할**:
- HTTP 요청을 받아서 처리
- 요청 데이터를 VO로 변환
- Service를 호출하여 비즈니스 로직 실행
- 결과를 `ApiResponse`로 래핑하여 JSON 응답

**특징**:
- 비즈니스 로직을 포함하지 않음
- 데이터 변환과 응답 형식만 담당

**예시**:
```java
@RestController
@RequestMapping("/users")
public class UserController {
  
  @GetMapping("/{userNo}")
  public ResponseEntity<ApiResponse<UserVO>> getUser(@PathVariable Long userNo) {
    UserVO userVO = userService.findUserById(userNo);
    return ResponseEntity.ok(ApiResponse.success(userVO));
  }
}
```

### 2. Service 계층 (비즈니스 로직 계층)

**위치**: `domain/*/Service.java`

**역할**:
- 비즈니스 로직 처리
- Entity와 VO 간 변환
- 트랜잭션 관리
- 예외 처리

**특징**:
- `@Transactional`로 트랜잭션 관리
- Repository를 호출하여 데이터 접근
- 비즈니스 규칙 검증

**예시**:
```java
@Service
@Transactional(readOnly = true)
public class UserService {
  
  @Transactional
  public UserVO createUser(UserVO userVO) {
    // 이메일 중복 검증
    if (userRepository.findByUserEml(userVO.getUserEml()).isPresent()) {
      throw new BusinessException(...);
    }
    
    // 비밀번호 해싱
    userVO.setEnpswd(passwordHelper.encode(userVO.getPassword()));
    
    // 저장
    UserEntity entity = userVO.toEntity();
    UserEntity saved = userRepository.save(entity);
    return new UserVO(saved);
  }
}
```

### 3. Repository 계층 (데이터 접근 계층)

**위치**: `domain/*/Repository.java`

**역할**:
- 데이터베이스에 접근하여 데이터 조회, 저장, 수정, 삭제
- JPA를 사용하여 쿼리 실행

**특징**:
- 인터페이스로 정의 (구현체는 Spring Data JPA가 자동 생성)
- 메서드 이름만으로 쿼리 자동 생성
- `JpaSpecificationExecutor`로 동적 쿼리 지원

**예시**:
```java
@Repository
public interface UserRepository 
    extends JpaRepository<UserEntity, Long>, 
            JpaSpecificationExecutor<UserEntity> {
  
  Optional<UserEntity> findByUserEml(String userEml);
  
  Page<UserEntity> findByUserNmContainingAndDelYn(
      String userNm, String delYn, Pageable pageable);
}
```

### 4. Entity 계층 (데이터 모델 계층)

**위치**: `domain/*/Entity.java`

**역할**:
- 데이터베이스 테이블과 매핑
- JPA 어노테이션으로 테이블 구조 정의
- 비즈니스 로직 메서드 포함 가능

**특징**:
- `CommonEntity`를 상속받아 공통 필드 포함
- `@Entity`, `@Table`, `@Id` 등 JPA 어노테이션 사용

**예시**:
```java
@Entity
@Table(name = "users")
public class UserEntity extends CommonEntity {
  
  @Id
  @GeneratedValue(strategy = GenerationType.IDENTITY)
  private Long userNo;
  
  @Column(name = "user_eml")
  private String userEml;
  
  // 비즈니스 로직 메서드
  public void recordLoginSuccess(String clientIp) {
    this.lgnFailNmtm = 0;
    this.lastLgnDt = LocalDateTime.now();
    this.lastLgnIp = clientIp;
  }
}
```

### 5. VO 계층 (데이터 전달 계층)

**위치**: `domain/*/VO.java`

**역할**:
- 클라이언트와 서버 간 데이터 전달
- API 요청/응답에 사용

**특징**:
- `CommonVO`를 상속받아 공통 필드 포함
- Entity와 변환 메서드 제공

**예시**:
```java
@Data
public class UserVO extends CommonVO {
  
  private Long userNo;
  private String userEml;
  private String userNm;
  
  // Entity → VO 변환
  public UserVO(UserEntity userEntity) {
    this.userNo = userEntity.getUserNo();
    this.userEml = userEntity.getUserEml();
    // ...
  }
  
  // VO → Entity 변환
  public UserEntity toEntity() {
    return UserEntity.builder()
        .userEml(this.userEml)
        .userNm(this.userNm)
        .build();
  }
}
```

---

## 주요 패턴과 원칙

### 1. 계층 분리 (Layered Architecture)

**원칙**: 각 계층은 명확한 책임을 가지며, 상위 계층만 하위 계층에 의존합니다.

```
Controller → Service → Repository → Entity
```

**장점**:
- 코드 재사용성 향상
- 테스트 용이성
- 유지보수성 향상

### 2. DTO 패턴 (Entity ↔ VO 변환)

**원칙**: Entity와 VO를 분리하여 각각의 목적에 맞게 사용합니다.

**Entity**: 데이터베이스와 직접 연결
**VO**: API 요청/응답에 사용

**장점**:
- 보안: 민감한 정보를 VO에서 제외 가능
- 유연성: Entity 구조 변경이 API에 영향 없음
- 명확성: 각 레이어의 책임이 명확함

### 3. 소프트 삭제 (Soft Delete)

**원칙**: 데이터를 실제로 삭제하지 않고 `delYn` 플래그로 표시합니다.

**장점**:
- 데이터 복구 가능
- 삭제 이력 추적 가능
- 참조 무결성 유지

### 4. 공통 필드 상속 (CommonEntity/CommonVO)

**원칙**: 모든 엔티티와 VO가 공통 필드를 상속받습니다.

**장점**:
- 코드 중복 제거
- 일관된 데이터 관리
- 감사(Audit) 필드 자동 관리

### 5. 전역 예외 처리 (GlobalExceptionHandler)

**원칙**: 모든 예외를 한 곳에서 일관되게 처리합니다.

**장점**:
- 일관된 에러 응답 형식
- 예외 처리 코드 중복 제거
- 에러 로깅 중앙화

### 6. 공통 응답 구조 (ApiResponse)

**원칙**: 모든 API 응답을 `ApiResponse`로 래핑합니다.

**장점**:
- 일관된 응답 형식
- 클라이언트 처리 용이
- 에러 코드 표준화

---

## 어노테이션 정리

### Spring Framework 어노테이션

| 어노테이션 | 용도 | 사용 위치 |
|-----------|------|----------|
| `@SpringBootApplication` | Spring Boot 애플리케이션 | 메인 클래스 |
| `@EnableJpaAuditing` | JPA 감사 기능 활성화 | 메인 클래스 |
| `@Configuration` | 설정 클래스 | Config 클래스 |
| `@EnableWebSecurity` | Spring Security 활성화 | SecurityConfig |
| `@Component` | 빈으로 등록 | 유틸리티 클래스 |
| `@Service` | 서비스 계층 빈 | Service 클래스 |
| `@Repository` | 데이터 접근 계층 빈 | Repository 인터페이스 |
| `@RestController` | REST API 컨트롤러 | Controller 클래스 |
| `@RequestMapping` | 공통 URL 경로 | Controller 클래스 |
| `@GetMapping` | HTTP GET 요청 처리 | Controller 메서드 |
| `@PostMapping` | HTTP POST 요청 처리 | Controller 메서드 |
| `@PatchMapping` | HTTP PATCH 요청 처리 | Controller 메서드 |
| `@DeleteMapping` | HTTP DELETE 요청 처리 | Controller 메서드 |
| `@PathVariable` | URL 경로 변수 | Controller 메서드 파라미터 |
| `@RequestParam` | 쿼리 파라미터 | Controller 메서드 파라미터 |
| `@RequestBody` | 요청 본문 (JSON) | Controller 메서드 파라미터 |
| `@Valid` | 입력값 검증 | Controller 메서드 파라미터 |
| `@Transactional` | 트랜잭션 관리 | Service 메서드 |
| `@Value` | 설정 값 주입 | 필드 |
| `@RequiredArgsConstructor` | final 필드 생성자 생성 | 클래스 |
| `@ExceptionHandler` | 예외 처리 | GlobalExceptionHandler 메서드 |
| `@RestControllerAdvice` | 전역 예외 처리 | GlobalExceptionHandler 클래스 |

### JPA 어노테이션

| 어노테이션 | 용도 | 사용 위치 |
|-----------|------|----------|
| `@Entity` | JPA 엔티티 | Entity 클래스 |
| `@Table` | 테이블 이름 지정 | Entity 클래스 |
| `@Id` | 기본 키 | Entity 필드 |
| `@GeneratedValue` | 기본 키 자동 생성 | Entity 필드 |
| `@Column` | 컬럼 매핑 | Entity 필드 |
| `@ManyToOne` | 다대일 관계 | Entity 필드 |
| `@OneToMany` | 일대다 관계 | Entity 필드 |
| `@JoinColumn` | 외래 키 컬럼 지정 | Entity 필드 |
| `@Enumerated` | Enum 타입 매핑 | Entity 필드 |
| `@Index` | 인덱스 생성 | Entity 클래스 |
| `@MappedSuperclass` | 상속 매핑 | 추상 클래스 |
| `@EntityListeners` | 엔티티 리스너 | Entity 클래스 |
| `@CreatedDate` | 생성 일시 자동 설정 | Entity 필드 |
| `@LastModifiedDate` | 수정 일시 자동 설정 | Entity 필드 |

### Lombok 어노테이션

| 어노테이션 | 용도 | 사용 위치 |
|-----------|------|----------|
| `@Getter` | getter 메서드 생성 | 클래스, 필드 |
| `@Setter` | setter 메서드 생성 | 클래스, 필드 |
| `@Data` | @Getter + @Setter + @ToString + @EqualsAndHashCode | 클래스 |
| `@NoArgsConstructor` | 기본 생성자 생성 | 클래스 |
| `@AllArgsConstructor` | 전체 필드 생성자 생성 | 클래스 |
| `@RequiredArgsConstructor` | final 필드 생성자 생성 | 클래스 |
| `@Builder` | 빌더 패턴 생성 | 클래스, 생성자 |
| `@Slf4j` | 로거 생성 | 클래스 |

---

## 학습 가이드

### 입문자를 위한 학습 순서

1. **기본 개념 이해**
   - [01. 프로젝트 시작과 기본 구조](./01_프로젝트_시작과_기본_구조.md)
   - [02. 공통 엔티티와 VO](./02_공통_엔티티와_VO.md)

2. **기본 기능 구현**
   - [03. 사용자 관리 기능 구현하기](./03_사용자_관리_기능_구현하기.md)

3. **인증 시스템**
   - [04. 인증 시스템 구현하기](./04_인증_시스템_구현하기.md)

4. **공통 기능**
   - [05. 공통 응답 구조와 예외 처리](./05_공통_응답_구조와_예외_처리.md)

5. **도메인 기능**
   - [06. 전역 풀 엔티티 구현하기](./06_전역_풀_엔티티_구현하기.md)

6. **전체 정리**
   - [07. 프로젝트 구조 정리](./07_프로젝트_구조_정리.md) (현재 문서)

### 실습 추천

1. **코드 읽기**: 각 튜토리얼을 읽으면서 실제 코드를 함께 확인
2. **API 테스트**: Postman이나 Swagger UI로 API를 직접 호출해보기
3. **디버깅**: 브레이크포인트를 설정하고 실행 흐름을 따라가보기
4. **작은 기능 추가**: 기존 코드를 참고하여 새로운 기능을 추가해보기

### 추가 학습 자료

- **Spring Boot 공식 문서**: https://spring.io/projects/spring-boot
- **Spring Data JPA 공식 문서**: https://spring.io/projects/spring-data-jpa
- **Spring Security 공식 문서**: https://spring.io/projects/spring-security
- **JWT 공식 문서**: https://jwt.io/

---

## 마무리

이 튜토리얼 시리즈를 통해 다음을 학습했습니다:

1. ✅ Spring Boot 프로젝트 구조 이해
2. ✅ JPA를 사용한 데이터베이스 연동
3. ✅ 계층 분리 아키텍처 (Controller-Service-Repository)
4. ✅ JWT 기반 인증 시스템
5. ✅ 공통 응답 구조와 예외 처리
6. ✅ 전역 풀 엔티티 구현

이제 프로젝트의 전체 구조와 각 계층의 역할을 이해했으니, 새로운 기능을 추가하거나 기존 코드를 수정할 수 있습니다.

**다음 단계**:
- 프로젝트의 다른 부분을 탐색해보세요
- 작은 기능을 추가해보세요
- 코드를 수정하고 테스트해보세요

행운을 빕니다! 🚀
